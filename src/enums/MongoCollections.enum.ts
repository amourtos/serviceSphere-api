export enum MongoCollections {
  BOARD_STICKIES = 'Board_Stickies',
  BOARD_REPLIES = 'board-replies',
  BOARDPOSTS = 'boardposts',
  IMAGES = 'images',
  USER_CREDENTIALS = 'user_credentials',
  USERS = 'users',
  VERIFICATION_CODES = 'verification_codes'
}
